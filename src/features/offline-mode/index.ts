/**
 * Feature: Low-Connectivity & Offline Sync Engine
 * 
 * Local-first offline queuing, FIFO background synchronization,
 * and resilient network mode switching (Normal, Low Connectivity, Offline Mesh).
 */

export { ConnectivityModal } from '../../components/ConnectivityModal';
export { offlineSyncManager } from '../../services/offlineSync';
